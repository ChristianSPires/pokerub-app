import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  notFoundPokemonsText: {
    marginTop: 20,
    textAlign: "center",
    fontSize: 20,
    fontWeight: "500",
  },
});
